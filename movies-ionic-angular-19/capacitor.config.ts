import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'movies-ionic-angular-18',
  webDir: 'www'
};

export default config;
